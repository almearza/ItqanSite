@model IEnumerable<Itqan.Models.Contact_Withme>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutCpanelRTL2.cshtml";
}


        @using (Html.BeginForm())
        {
            <table class="table container pb-5">
                <tr>
                    <th class="col-md-4">
                        @Html.DropDownList("Lang", null, htmlAttributes: new { @class = "form-control" })
                    </th>
                    <th class="col-md-2">
                        <input type="submit" value="بحث" class="btn btn-primary" style="max-width:150px;" />
                    </th>

                    @*<th class="col-md-4">
                        <a href="~/Contact_Withme/Create" class="btn btn-primary">إضافة</a>
                    </th>*@



                </tr>
            </table>

        }

<table class="table container text-center">
    <tr>
        <th>
            البريد الالكتروني
        </th>
       
        <th>
            الرسالة
       
        
        
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
      
        <td>
            @Html.DisplayFor(modelItem => item.Message)
        </td>
       

        @*<td>
            @Html.ActionLink("تعديل", "Edit", new { id=item.ID_contact }) 

            @Html.ActionLink("Delete", "Delete", new { id=item.ID_contact })
        </td>*@
        <td>
            @Html.ActionLink("تفاصيل", "Details", new { id = item.ID_contact })
        </td>
    </tr>
}

</table>
